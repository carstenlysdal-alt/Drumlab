[build]
buildCommand = "npm run build"

[deploy]
startCommand = "HOSTNAME=0.0.0.0 ./node_modules/.bin/next start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
